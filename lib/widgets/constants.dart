import 'list_section.dart';

/// [T] : Widget || routeName ， String routeName 。
class Constants<T> extends ListSection<T> {
  const Constants(super.children, {super.key}) : super(title: '常量 Constants');
}
